import { render } from "@testing-library/vue";
import PicCard from "../src/pic-card";

describe("pic-card test", () => {
  test("pic-card init render", async() => {
    // const { getByRole } = render(PicCard);
    // getByRole("pic-card");
  });
});
